import Mainarticles from '../LandingPage/mainarticles';
import React from 'react';

describe('test should render component', () => {
    it('renders the <Mainarticles /> component', () => {
        shallow(<Mainarticles />);
    });
});
