import latestarticles from '../LandingPage/latestarticles';
import React from 'react';

describe('test should render component', () => {
    it('renders the <latestarticles /> component', () => {
        shallow(<latestarticles />);
    });
});
